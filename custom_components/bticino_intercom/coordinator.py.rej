--- custom_components/bticino_intercom/coordinator.py
+++ custom_components/bticino_intercom/coordinator.py
@@ -103,6 +103,7 @@
         # Last message timestamp to detect stale websocket connections
         # Use datetime.now(UTC) instead of time.time() for easier comparison with HA timestamps
         self._last_ws_message_time = datetime.now(UTC)
+        self._last_incoming_call_time = {} # module_id -> timestamp
 
         # Event loop tasks
         self._ws_connect_task = None
